direccion debe escribirse: localhost\SQLALE

puerto no se está cogiendo en modo Invitado, ni se envía en la connectionString

alter table no refresca nombre columnas una vez eliminadas

comprobar guardado contraseña y recuperación cuando checkbox contraseña marcado

añadir radiobutton PK a create table

condiciones where generadas, no borrar contenido existente

mirar doble espacio WHERE en VUpdate VDelete

añadir opcion "" a cmbAndOr where

VGenerica drop sacar comando desde cambiarModo, no desde constructor.